# 🎨 챗봇 위젯 색상 가이드

챗봇 위젯의 모든 색상은 아래 파일에서 관리됩니다.

**파일 위치**: `/home/tuf/web_server/templates/components/chatbot_widget.html`

---

## 📋 색상 구성 요소

### 1. 토글 버튼 (둥근 아이콘 버튼)
| 항목 | 라인 | CSS 속성 | 현재 값 |
|---|---|---|---|
| 배경색 | 13번 줄 | `background` | `#2d3136` |
| 그림자 | 16번 줄 | `box-shadow` | `rgba(45, 49, 54, 0.5)` |
| 호버 그림자 | 27번 줄 | `box-shadow` | `rgba(45, 49, 54, 0.7)` |

```css
.chatbot-toggle {
    background: #2d3136;  /* ← 버튼 배경색 */
    box-shadow: 0 4px 15px rgba(45, 49, 54, 0.5);  /* ← 그림자 */
}
```

---

### 2. 채팅창 전체 외곽
| 항목 | 라인 | CSS 속성 | 현재 값 |
|---|---|---|---|
| 배경색 | 49번 줄 | `background` | `#000000ff` |
| 테두리 그림자 | 51번 줄 | `box-shadow` | `rgba(0, 0, 0, 0.3)` |

```css
.chatbot-window {
    background: #000000ff;  /* ← 채팅창 외곽 배경 */
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);  /* ← 테두리 그림자 */
}
```

---

### 3. 헤더 (제목 영역)
| 항목 | 라인 | CSS 속성 | 현재 값 |
|---|---|---|---|
| 배경색 | 76번 줄 | `background` | `#2d3136` |
| 글자색 | 77번 줄 | `color` | `white` |

```css
.chatbot-header {
    background: #2d3136;  /* ← 헤더 배경색 */
    color: white;  /* ← 헤더 글자색 */
}
```

---

### 4. 메시지 영역 (대화 표시 영역)
| 항목 | 라인 | CSS 속성 | 현재 값 |
|---|---|---|---|
| 배경색 | 109번 줄 | `background` | `#f1f1f1ff` |

```css
.chatbot-messages {
    background: #f1f1f1ff;  /* ← 메시지 영역 배경색 */
}
```

---

### 5. 사용자 말풍선
| 항목 | 라인 | CSS 속성 | 현재 값 |
|---|---|---|---|
| 배경색 | 136번 줄 | `background` | `#2d3136` |
| 글자색 | 137번 줄 | `color` | `white` |

```css
.chat-message.user .chat-bubble {
    background: #2d3136;  /* ← 사용자 말풍선 배경 */
    color: white;  /* ← 사용자 말풍선 글자색 */
}
```

---

### 6. 봇 말풍선
| 항목 | 라인 | CSS 속성 | 현재 값 |
|---|---|---|---|
| 배경색 | 142번 줄 | `background` | `white` |
| 글자색 | 143번 줄 | `color` | `#333` |

```css
.chat-message.bot .chat-bubble {
    background: white;  /* ← 봇 말풍선 배경 */
    color: #333;  /* ← 봇 말풍선 글자색 */
}
```

---

### 7. 입력창 영역
| 항목 | 라인 | CSS 속성 | 현재 값 |
|---|---|---|---|
| 배경색 | 151번 줄 | `background` | `white` |
| 테두리색 | 160번 줄 | `border` | `#ddd` |
| 포커스 테두리 | 168번 줄 | `border-color` | `#2d3136` |

```css
.chatbot-input {
    background: white;  /* ← 입력창 영역 배경 */
}
.chatbot-input input {
    border: 1px solid #ddd;  /* ← 입력창 테두리 */
}
.chatbot-input input:focus {
    border-color: #2d3136;  /* ← 포커스 시 테두리 */
}
```

---

### 8. 전송 버튼
| 항목 | 라인 | CSS 속성 | 현재 값 |
|---|---|---|---|
| 배경색 | 173번 줄 | `background` | `#2d3136` |
| 호버 배경색 | 183번 줄 | `background` | `#3d4248` |
| 비활성화 배경 | 188번 줄 | `background` | `#ccc` |

```css
.chatbot-input button {
    background: #2d3136;  /* ← 전송 버튼 배경 */
}
.chatbot-input button:hover {
    background: #3d4248;  /* ← 호버 시 배경 */
}
```

---

## 🎨 유용한 색상 팔레트

```
다크 그레이:  #2d3136
밝은 그레이:  #3d4248
연한 회색:    #f1f1f1
흰색:         #ffffff
검정:         #000000
오렌지:       #ff6b35
민트 그린:    #10b981
퍼플:         #8b5cf6
```
